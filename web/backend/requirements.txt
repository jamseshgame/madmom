fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic-settings>=2.1
httpx>=0.27
packaging>=23.0
Pillow>=10.0
# Anthropic SDK for the "Propose new presets from feedback" button (chart
# iteration loop). 0.40+ has the messages API + prompt-caching support.
anthropic>=0.40
# numpy/torch/torchaudio pinned in lockstep — torch wheels bind to a specific
# numpy ABI, and bumping any of them in isolation breaks demucs at runtime.
# Bump together after verifying the trio still resolves on PyPI for both
# Linux (droplet) and Windows (dev). Cython .so files compiled against an
# older numpy ABI must be rebuilt after a numpy major bump — deploy reruns
# `setup.py build_ext --inplace`.
#
# ALSO capped by numba (transitive dep of basic-pitch): NumPy 2.5+ makes numba
# fail to import with "Numba needs NumPy 2.4 or less", killing the basic-pitch
# engines. This exact pin is mirrored in constraints.txt (applied via `-c` to
# every install) so a stray `pip install` can't drag numpy up past it.
numpy==2.4.4
demucs>=4.0
# ── audio-separator runtime deps ────────────────────────────────────────────
# python-audio-separator powers the Roformer / MDX-Net / VR Arch engines (see
# app/services/separators.py) — one package, four model families, including
# BS-Roformer and Mel-Band Roformer, which beat Demucs on vocals by a wide
# margin. The package ITSELF lives in requirements-extras.txt because it hard-
# requires diffq (diffq-fixed on Windows), whose sdist no longer builds; we
# install it --no-deps and satisfy its real dependencies here instead. diffq is
# only imported by audio-separator's *bundled Demucs bridge*, which we never
# use — the standalone `demucs` package above is its own engine.
#
# Checkpoints are NOT bundled: they download on first use into
# `audio_separator_model_dir` (a few hundred MB each). If any of this is
# missing the separator engines degrade gracefully — the catalog endpoint
# reports them unavailable and Demucs keeps working.
onnxruntime>=1.17      # swap for onnxruntime-gpu / onnxruntime-directml
ml_collections
rotary-embedding-torch>=0.6.1,<0.7.0
einops>=0.7
julius>=0.2
pydub>=0.25
resampy>=0.4
samplerate==0.1.0
audioop-lts>=0.2.1; python_version >= "3.13"
# audio-separator's metadata pins beartype<0.19, but 0.18.x cannot parse the
# `Callable | None` hint on BSRoformer.__init__ under Python 3.13+ and every
# Roformer load dies with a BeartypeDecorHintNonpepException. 0.19+ handles it.
beartype>=0.19
# NOTE: MDX-Net support (torchvision + onnx2torch + onnx-weekly) is deliberately
# NOT here — it lives in requirements-extras.txt, installed --no-deps. Those
# packages pull in a *specific* torch build (torchvision hard-pins a torch
# patch), so listing them here alongside the pinned torch would force a
# torch/torchcodec reinstall on any box whose torch patch differs — exactly the
# breakage the constraints file exists to prevent. Roformer/MDXC and VR Arch —
# the default hybrid engine and every top vocal model — need none of them, so
# MDX-Net is treated as an optional bonus that never risks the torch install.
#
# Pinned to the minor line, not an exact patch: the dev box runs 2.12.0 and the
# droplet 2.12.1 — both work, and an exact `==2.12.0` forces a torchcodec-
# breaking downgrade on the droplet. `.*` blocks the minor bump (the thing that
# actually breaks torchcodec) while tolerating the patch. Mirrored in
# constraints.txt.
torch==2.12.*
torchaudio==2.11.*
# torchaudio >= 2.5 dispatches save()/load() through torchcodec instead of the
# built-in soundfile/sox backends. demucs 4.x still calls torchaudio.save(),
# so torchcodec must be present or every separation crashes at the final
# stem-write step with "TorchCodec is required for save_with_torchcodec".
torchcodec
# YouTube search + download for the "search YouTube" upload flow. yt-dlp uses
# the system ffmpeg for the mp3 extraction post-processor, which we already
# install at the OS level. The [default] extra pulls in yt-dlp-ejs, the
# challenge-solver scripts yt-dlp needs to decrypt YouTube stream URLs; those
# scripts also need a JS runtime on PATH — Deno (recommended; Node 20 is too
# old) — or every download fails with "Requested format is not available".
yt-dlp[default]>=2026.3
# Chatterbox TTS lives in requirements-extras.txt — it pins torch==2.6.0 and
# numpy<2.0.0 in its metadata, which would block the resolver from
# installing the trio above. We install it separately with --no-deps so the
# pins are ignored; runtime works fine on torch 2.12 / numpy 2.
# Whisper transcription (CTranslate2-backed, CPU-friendly, MIT). Lazy-loaded
# on first /api/lyrics/whisper call. Default model "medium" is ~1.5 GB and
# downloads on first use.
faster-whisper>=1.0
pytest-asyncio>=0.23
# Pitch detection for vocal beatmaps. CPU-runnable; first call downloads
# the ~30 MB CREPE 'full' model from HuggingFace.
torchcrepe>=0.0.23
# English syllable splitter for vocal beatmap note granularity. Sonority
# Sequencing Principle (SSP) — orthographic syllable splits where pyphen's
# typographic-hyphen output left short words like "Hello" unsplit.
syllabipy>=0.2
# Password hashing for the multi-user account store.
bcrypt>=4.0
# Audio loading via librosa (resampling, mono downmix).
librosa>=0.10.1
soundfile>=0.12.1
